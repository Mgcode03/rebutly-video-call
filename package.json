{
  "name": "video-call-app",
  "version": "1.0.0",
  "description": "One-to-one video call application with Firebase",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "vite": "^5.0.10"
  }
}
